@model Dfc.CourseDirectory.Web.Components.LarsSearchResult.LarsSearchResultModel;

@if (Model.HasErrors)
{
    <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="error-summary">
        <h2 class="govuk-error-summary__title" id="error-summary-title">
            Sorry, there was a problem.
        </h2>
        <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">
                @foreach (var error in Model.Errors)
                {
                    <li>
                        @error
                    </li>
                }
            </ul>
        </div>
    </div>
}
else
{
    <table class="govuk-table">
        <caption class="govuk-table__caption">Search results for: @Model.SearchTerm</caption>
        <thead class="govuk-table__head">
            <tr class="govuk-table__row">
                <th class="govuk-table__header" scope="col">Learning Ain Ref.</th>
                <th class="govuk-table__header" scope="col">Learning Aim Title</th>
                <th class="govuk-table__header" scope="col">NVQ Level V2</th>
                <th class="govuk-table__header" scope="col">Award Org.</th>
            </tr>
        </thead>
        <tbody class="govuk-table__body">
            @if (Model.Items.Count() > 0)
            {
                foreach (var item in Model.Items)
                {
                    <tr class="govuk-table__row">
                        <td class="govuk-table__cell">@item.LearnAimRef</td>
                        <td class="govuk-table__cell">@item.LearnAimRefTitle</td>
                        <td class="govuk-table__cell">@item.NotionalNVQLevelv2</td>
                        <td class="govuk-table__cell">@item.AwardOrgName (@item.AwardOrgCode)</td>
                    </tr>
                }
            }
            else
            {
                <tr class="govuk-table__row">
                    <td class="govuk-table__cell" colspan="4">No results</td>
                </tr>
            }
        </tbody>
    </table>
}