@addTagHelper *, GovUk.Frontend.AspNetCore
@model Dfc.CourseDirectory.Web.ViewModels.BulkUpload.PublishCompleteViewModel
@{
    Layout = "_V2LayoutProviderContext";
    ViewBag.Title = "Publish complete";
}

<govuk-panel>
    <govuk-panel-title>Publish complete</govuk-panel-title>

    You have @Model.NumberOfCoursesPublished @(Model.NumberOfCoursesPublished == 1 ? "course" : "courses") listed on the course directory
</govuk-panel>
