using Dfc.CourseDirectory.Models.Interfaces.Providers;
using Dfc.CourseDirectory.Models.Interfaces.Qualifications;
using Dfc.CourseDirectory.Models.Interfaces.Venues;
using Dfc.CourseDirectory.Models.Models.Courses;
using Dfc.CourseDirectory.Models.Models.Providers;
using Dfc.CourseDirectory.Models.Models.Qualifications;
using Dfc.CourseDirectory.Models.Models.Venues;
using System;
using Xunit;

namespace Dfc.CourseDirectory.Models.Test
{
    public class CourseTests
    {
        [Fact]
        public void Create_Course()
        {
            //Contactaddress address = new Contactaddress();
            //Contactpersonaldetails details = new Contactpersonaldetails();
            //Provideralias[] alias = new Provideralias[1];
            //Verificationdetail[] verDetail = new Verificationdetail[1];
            //Providercontact[] contact = new Providercontact[1];
            //Provider provider = new Provider(contact, alias, verDetail);

            //IVenue venue = new Venue("s1", 10000001, 2, 3, "ss", "ss", "ss", "ss", "ss", "ss", "ss", "ss",
            //                         VenueStatus.Live, "me", DateTime.Now, DateTime.Now);
            //DateTime[] times = new DateTime[20180411];
            ////CourseInformation info = new CourseInformation(
            ////    times,
            ////    "StudyMode",
            ////    "Attendance",
            ////    "CourseID",
            ////    "CourseURL",
            ////    "Pattern",
            ////    "Requirements");
            ////CourseRun data = new CourseRun(venue, info);

            //Qualification qual = new Qualification("ss", "ss", "ss", "ss", "ss", "ss", "ss", "ss", "ss", "ss");

            ////CourseText text = new CourseText(
            ////    "CourseTitle",
            ////    "Learn",
            ////    "How",
            ////    "Why"
            ////    );

            //CourseRun courserun = new CourseRun("s1", "s2", "s3", "s4", "s5", "s6", "s7", "s8", "s9", "s10", "s11",
            //                                    false, DateTime.Now, "s12", 123.45m, "s13", true, DurationUnit.Day, 5,
            //                                    StudyMode.FullTime, AttendancePattern.Daytime, venue, provider, qual, DateTime.Now, "him", DateTime.Now, "her");

            //QuAP quap = new QuAP(
            //    id: Guid.NewGuid(),
            //    qualification: qual,
            //    provider: provider
            //);

            //CourseData data = new CourseData(Guid.NewGuid(), Guid.NewGuid(), "course title");

            //Course course = new Course(
            //    id: Guid.NewGuid(),
            //    quAP: quap,
            //    data: data,
            //    run: new CourseRun[] { courserun }
            //    );

            //Assert.True(true);
            ////Assert.NotNull(course);
        }

    }
}